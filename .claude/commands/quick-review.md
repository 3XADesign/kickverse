Review the most recent code changes for:

1. Security vulnerabilities:
   - SQL injection (check prepared statements)
   - XSS attacks (check output escaping)
   - CSRF protection (check tokens)
   - Hardcoded credentials
   - File upload vulnerabilities

2. Performance issues:
   - Slow database queries (missing indexes)
   - N+1 query patterns
   - Large file sizes
   - Missing caching

3. Code quality:
   - Error handling
   - Input validation
   - Code consistency
   - Best practices

4. Database optimization:
   - Missing indexes
   - Inefficient queries
   - Proper data types

Provide a clear report with:
- ✅ What's good
- ⚠️ Warnings that should be fixed
- ❌ Critical issues that must be fixed

Suggest specific fixes for each issue found.
